import 'package:flutter/cupertino.dart';

import '../DataStatsDialog.dart';

/*class LottoStatisticsChart extends StatefulWidget {
  static int round645 = 1;
  static int round720 = 1;

  //const ListPage({super.key});

  @override
  LottoChartBuilder createState() => LottoChartBuilder();
}

class LottoChartBuilder extends State<LottoStatisticsChart> {
  int touchedIndex = -1;

  @override
  Widget build(BuildContext context) {
    return AspectRatio(
      aspectRatio: 1,
        child: Column(
        children: <Widget>[
        const SizedBox(
        height: 28,
    ),
    Row(
    mainAxisAlignment: MainAxisAlignment.spaceEvenly,
    Indicator(
    color: AppColors.contentColorBlue,
    text: 'One',
    isSquare: false,
    size: touchedIndex == 0 ? 18 : 16,
    textColor: touchedIndex == 0
    ? AppColors.mainTextColor1
        : AppColors.mainTextColor3,
    ),



    )


        ]

    );
  }


}*/
